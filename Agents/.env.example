# Neo4j AuraDB Configuration Template
# Copy this file to .env and fill in your actual credentials

NEO4J_URI=neo4j+s://your-instance.databases.neo4j.io
NEO4J_USERNAME=neo4j
NEO4J_PASSWORD=your-password-here

# Optional: Set batch sizes for loading (default values shown)
NODES_BATCH_SIZE=1000
EDGES_BATCH_SIZE=5000

# Instructions:
# 1. Go to https://console.neo4j.io/ and create a new AuraDB instance
# 2. Copy the connection URI (starts with neo4j+s://)
# 3. Use 'neo4j' as username (default)
# 4. Use the password you set when creating the database
# 5. Save this file as .env (without .example extension)